import { describe, it } from "vitest";

/**
 * The original ItemForm.tsx was replaced by item-form/ItemForm.tsx.
 * Tests for the new ItemForm are in item-form/ItemForm.test.tsx.
 * This stub ensures vitest doesn't complain about a test file with no suites.
 */
describe("ItemForm (legacy stub)", () => {
  it("tests moved to item-form/ItemForm.test.tsx", () => {
    // no-op: the real tests are in item-form/ItemForm.test.tsx
  });
});
